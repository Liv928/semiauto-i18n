查找替换中文
正则表达式 ([\u4e00-\u9fa5]+)
替换字符串 $t('$1')

查找替换label和placeholder等
正则表达式 (\w+)="\$t\('
替换字符串 :$1="$t('


我现在希望你写一个js脚本，处理vue文件的国际化，实现如下功能，具体操作如下：1.提取所有汉字串，并生成一个json文件，格式为”GE.汉字“=”汉字“。2.原文件中template大标签下的所有内容取出，生成一个新文件，并将其中的汉字按如下规则替换：被双引号包围的汉字字符串，如："汉字"，改为"$t('GE.汉字')"的格式；没被双引号包围的汉字，如 汉字 ， 改为 {{ $t('GE.汉字') }}。在这之后，处理部分英文单词，使用正则表达式来匹配所有形如 word="$t(' 的字符串，并将其替换为 :word="$t(' 的格式。3.将原文件中script大标签下的所有内容取出，生成一个新文件，其中的汉字字符串一定被单引号包裹，按如下规则替换：如'汉字'，替换为this.$t('GE.汉字')